const add = (n, i) => n + i;
const subtract = (n, i) => n - i;
const divide = (n, i) => n / i;
const multiply = (n, i) => n * i;
const squareRoot = (n) => Math.sqrt(n);
const max = (n) => Math.max(n);

module.exports = {
    add,
    subtract,
    divide,
    multiply,
    squareRoot,
    max
}